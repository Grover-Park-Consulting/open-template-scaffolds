# Build Record: Prompt Naming Alignment + Missing-Prompt Check

**Who reads this:** George (and any future AI assistant working in this library). The fourth
implementation task, per `build-records/2026-09-26-task-brief-prompt-naming.md`.

**Author:** the AI assistant (deepseek-v4-pro), via OpenCode.
**Date/time:** 2026-09-26 10:36.

**Frozen files (`FREEZE.md`, the 18): no.** I touched none of them. `prompts/BuildNewTables-StartHere.md`
— the one frozen file in `prompts/` — was left untouched.

**Part 2 (the "missing prompt" finding): the original finding was a false positive.** There is no
real gap. Detail below.

---

## Part 1 — naming alignment (5 renames)

Rule applied, per the brief: each prompt's domain word becomes PascalCase of its template slug's
name part (slug minus the type suffix). Types were read from front-matter, not filenames; slugs were
read from `templates/_COVERAGE.md`.

| Old filename | New filename | Slug it starts |
|---|---|---|
| `AssetTracking-OutcomeFirst-StartHere.md` | `CapitalAssetTracking-OutcomeFirst-StartHere.md` | `capital-asset-tracking-outcome-first` |
| `AuditLogging-OutcomeFirst-StartHere.md` | `AuditLoggingLite-OutcomeFirst-StartHere.md` | `audit-logging-lite-outcome-first` |
| `AuditLogging-Scaffold-StartHere.md` | `AuditLoggingLite-Scaffold-StartHere.md` | `audit-logging-lite-scaffold` |
| `LibraryCatalog-OutcomeFirst-StartHere.md` | `Catalog-OutcomeFirst-StartHere.md` | `catalog-outcome-first` |
| `LibraryCatalog-FormSpec-StartHere.md` | `Publication-FormSpec-StartHere.md` | `publication-form` |

The other ten prompts already matched their slugs (`AppStartup`, `ErrorLogging`,
`OfficiatingAssignment`, `RecordFinder`, `StocktakeScan`) and were left alone.

**The four renames beyond the brief's named example are my reading of "every".** The brief names only
`AssetTracking`. Applying the same rule to all prompts also changes audit (`AuditLogging` →
`AuditLoggingLite`, because the slug carries `lite`) and library (`LibraryCatalog` → `Catalog` for the
outcome-first, `Publication` for the form-spec). Flags for you:

- **Audit** — the slug is `audit-logging-lite-*`; `AuditLogging` omits `Lite`. If the prompt's domain
  word is meant to be the short domain ("audit logging") rather than the full slug name part, these
  two should revert.
- **Library** — the two `LibraryCatalog` prompts map to *different* slugs (`catalog-outcome-first`
  and `publication-form`), so they get *different* new domain words (`Catalog` and `Publication`). If
  the library domain is meant to keep one shared word, this is the wrong shape.

No file content changed — the prompts reference templates, not other prompts.

## Part 2 — the missing-prompt finding

The earlier review flagged "no scaffold prompt for `asset-tracking` (only outcome-first)". Checked
against `templates/_COVERAGE.md`: asset-tracking has **no** `vba-scaffold` template at all (its row
shows a dash in that column). There is nothing for a scaffold prompt to start, so **the finding is a
false positive** and no prompt was created.

**No other domain has a gap.** All 15 shipped non-`table-schema` templates have a matching prompt
(often two, one per route); the 7 `table-schema` templates have no prompt by design — the generic
`BuildNewTables-StartHere.md` serves them.

## Reference check

- `README.md` (frozen): checked — no reference to any renamed prompt. Nothing to name.
- `CLAUDE.md`: checked — no reference to any renamed prompt.
- Template bodies: checked — no template names a paired prompt.
- **Still naming an old filename, left as-is, for your call:** three *dated* build-records —
  `build-records/audit-logging-lite-outcome-first/2026-09-22-…`, `build-records/audit-logging-lite-scaffold/2026-09-22-…`,
  and `build-records/capital-asset-tracking-outcome-first/2026-09-23-…`. These sit under `build-records/*/`,
  which `.gitignore` marks "per-developer history, not library content", and they are dated narratives
  of past builds. I left them rather than rewrite a dated record's pointer — say the word and I'll
  update them. (The task brief itself also quotes the old `AssetTracking` name, as its subject matter.)

## Verification

- `python mcp-server/run_validate.py` → **21/21 well-formed**.
- `python -m unittest discover -s mcp-server/tests` → **31 OK, 1 skipped**.
- `git status -- prompts/` → five `D` (old) + five `??` (new); no other file touched by this task.
