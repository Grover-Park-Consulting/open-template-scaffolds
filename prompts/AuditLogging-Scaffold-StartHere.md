Build audit logging into my Access database using the set-up wizard.

Outcome: every change to the tables I choose gets recorded automatically by the database itself —
who, what field, before/after value — no matter how the change was made, with no way around it.

Resources — these files are inside the Open Template Scaffolds library folder; the paths below are
relative to that folder's root: templates/audit/audit-logging-lite-scaffold.md (the template),
templates/audit/audit-logging-lite-schema.md (paired tables), standards/ (my conventions),
README.md and templates/_template-schema.md for the rest.

A build is successful when all validation checks run.
A build is successful when all validation checks pass.
